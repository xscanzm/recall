# ASCII only: pip decodes this file with the system locale codec (gbk on zh-CN
# Windows), so non-ASCII comments break "pip install -r". Keep notes in English.
#
# opencv-python-headless, not opencv-python: the worker only calls
# cv2.setNumThreads(1) and all image processing happens inside rapidocr, so no
# GUI backend is ever used. On Windows the two wheels are nearly the same size
# (~175 KB apart) because neither bundles Qt -- this swap is about not shipping
# GUI entry points we never call, not about bytes. The real size win comes from
# deleting the videoio ffmpeg DLL in scripts/build-rapidocr-worker.ps1.
rapidocr==3.9.2
onnxruntime==1.27.0
antlr4-python3-runtime==4.9.3
certifi==2026.7.22
charset-normalizer==3.4.9
colorama==0.4.6
colorlog==6.11.0
flatbuffers==25.12.19
idna==3.18
numpy==2.4.6
omegaconf==2.3.1
opencv-python-headless==5.0.0.93
packaging==26.2
pillow==12.3.0
protobuf==7.35.1
pyclipper==1.4.0
pyyaml==6.0.3
requests==2.34.2
shapely==2.1.2
six==1.17.0
tqdm==4.69.0
urllib3==2.7.0
